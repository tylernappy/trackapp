<h1>Opponents#new</h1>
<div class="row">
  <div class="span6">
    <%= render "form" %>
  </div>
  <div class="span6">
    <% @opponents_already_created.each do |opponent|  %>
      <ul>
        <li><%= School.find_by_id(opponent.school_id).name %></li>
      </ul>
    <% end %>
  </div>
</div>

<%= link_to "Back", season_meet_path(@season, @meet), :class => 'btn' %>
