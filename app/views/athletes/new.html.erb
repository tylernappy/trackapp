<h1>New Athlete</h1>

<div class="row">
  
  <div class="span6">
    <%= render "form" %>
  </div>

  <div class="span6">
    <ul>
      <% @athletes_already_created.each do |athlete| %>
        <li><%= athlete.name %></li>      
      <% end %>
    </ul>
  </div>  

</div>
<%= link_to "Back", seasons_path, :class => 'btn' %>